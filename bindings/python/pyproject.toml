[project]
name = "obscura-runtime"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["psutil>=7,<8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/obscura_runtime"]
